import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// mocking need to be done for running test case, for now we have deleted testing code and created blank test
// because for checking crashing, it will redirect to fib and finally to express server,which might not be running
it('renders without crashing', () => {
});
